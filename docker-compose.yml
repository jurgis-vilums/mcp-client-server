version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SESSION_DB_PATH: /data/sessions.db
    volumes:
      - sessions-data:/data

volumes:
  sessions-data:
